# MyDecimal — реализация типа данных Decimal на C

## 📌 О проекте

**MyDecimal** — это статическая библиотека на языке C, реализующая собственный тип данных `decimal`, предназначенный для точных финансовых и математических вычислений без потерь точности, характерных для типов с плавающей запятой.

Библиотека предоставляет:
- Арифметические операции (сложение, вычитание, умножение, деление)
- Операции сравнения
- Преобразования из/в `int` и `float`
- Математические функции (округление, усечение, смена знака)

## 📂 Сборка

### Сборка библиотеки
```bash
make s21_decimal.a
```

### Запуск тестов
```bash
make test
```

### Генерация отчёта о покрытии кода (gcov)
```bash
make gcov_report
```

### Полная сборка (библиотека + тесты)
```bash
make all
```

### Очистка собранных файлов
```bash
make clean
```

## 🔧 Реализованные функции

### Арифметические операции
- `s21_add` — сложение
- `s21_sub` — вычитание
- `s21_mul` — умножение
- `s21_div` — деление

### Сравнения
- `s21_is_less`
- `s21_is_less_or_equal`
- `s21_is_greater`
- `s21_is_greater_or_equal`
- `s21_is_equal`
- `s21_is_not_equal`

### Конвертеры
- `s21_from_int_to_decimal`
- `s21_from_float_to_decimal`
- `s21_from_decimal_to_int`
- `s21_from_decimal_to_float`

### Другие функции
- `s21_floor` — округление вниз
- `s21_round` — математическое округление
- `s21_truncate` — отбрасывание дробной части
- `s21_negate` — смена знака

## ⚙️ Внутреннее представление

Тип `s21_decimal` реализован как структура:
```c
typedef struct {
    uint32_t bits[4];
} s21_decimal;
```
где:
- `bits[0]`, `bits[1]`, `bits[2]` — младшие, средние и старшие 32 бита 96-битного целого числа
- `bits[3]` — содержит знак и показатель степени (масштаб)

## 🧪 Тестирование

Проект включает unit-тесты с использованием библиотеки **Check**. Покрытие тестами составляет более 80% для каждой функции.

## 📄 Требования

- Язык: C11
- Компилятор: gcc
- Стандарт кодирования: Google C++ Style (адаптирован для C)
- Система сборки: Make
- Покрытие тестами: ≥80% на функцию
- Запрещено использование `__int128`

> Примечание: Библиотека совместима с POSIX-системами и предназначена для использования в приложениях, требующих высокой точности вычислений (финансы, бухгалтерия, научные расчёты).
